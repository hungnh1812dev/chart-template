# Layered on top of values-ci.yaml to exercise init containers.
initContainers:
  enabled: true
  containers:
    - name: migrate
      image: ghcr.io/acme/shop-api:1.2.3
      command: ["./migrate", "up"]
